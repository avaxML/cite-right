name: OpenWiki Update

# After each merge to main, plus manual runs. Daily cron is extra coverage,
# not a substitute for merge-to-main. Generated pages land on a PR branch;
# they are never pushed straight to main (branch protection). After a
# successful openwiki code --update, public paths are copied into docs/
# (existing mkdocs.yml nav). GitHub Pages deploys from docs/ on merge.
#
# Free OpenRouter models rotate at runtime. scripts/openwiki_pick_model.py
# lists ids containing :free (prefer tool-calling), ranked by Artificial
# Analysis coding_index when present, else context_length, then created.
# scripts/openwiki_update.sh exports OPENWIKI_MODEL_ID to the first id and
# retries the next on 429/402/rate-limit, 403/404, agentic-harness blocks,
# model-unavailable errors, and malformed completions (OpenWiki TypeError
# on undefined[0]). On 429 it sleeps retry_after (cap 90s).
# Do not pin a paid model id here.
on:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    # GitHub schedules use UTC; 08:00 UTC is midnight PST.
    - cron: "0 8 * * *"

permissions:
  contents: write
  pull-requests: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          persist-credentials: true
          # Full history so `openwiki code --update` can diff HEAD against the
          # commit it last documented; a shallow clone hides that commit and the
          # update runs against an empty change summary.
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: "22"

      - name: Install OpenWiki
        # mermaid + jsdom are optional; they add high-fidelity validation of Mermaid diagrams.
        run: npm install --global openwiki@latest mermaid@11.16.0 jsdom@29.1.1

      - name: Run OpenWiki
        # --update also handles the first run for code docs when env vars are set.
        # Model id comes from scripts/openwiki_update.sh (free OpenRouter rotation).
        run: bash scripts/openwiki_update.sh
        env:
          # OpenWiki sends anonymous reliability telemetry from CI runs.
          # Uncomment to disable:
          # OPENWIKI_TELEMETRY_DISABLED: "1"
          OPENWIKI_PROVIDER: openrouter
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}

      - name: Publish public pages to docs
        run: python3 scripts/publish_openwiki_to_docs.py

      - name: Create OpenWiki update pull request
        uses: peter-evans/create-pull-request@22a9089034f40e5a961c8808d113e2c98fb63676 # v7
        with:
          add-paths: |
            openwiki
            docs
            AGENTS.md
            CLAUDE.md
          branch: openwiki/update
          commit-message: "docs: update documentation from OpenWiki"
          title: "docs: update documentation from OpenWiki"
          body: |
            Automated documentation update.

            OpenWiki refreshed `openwiki/`. Public paths were copied into `docs/`
            for GitHub Pages (existing mkdocs.yml nav). Review, then merge; do
            not push generated files straight to main.
